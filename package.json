{
  "name": "static_chess",
  "private": true,
  "version": "0.0.0",
  "description": "Dev tooling for Static Chess (lint/test only)",
  "scripts": {
    "lint": "eslint . --ext .js",
    "format": "prettier --check .",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "jsdom": "^24.1.0",
    "prettier": "^3.3.3"
  }
}

